using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PickUpEffect : AbstractEffect
{
    public override bool Apply(AbstractCharacter target)
    {
        //TODO: make this one the functionality of all pickups, because only the MainCharacter is supposed to pick stuff up anyway.
        throw new System.NotImplementedException();
    }
}
